# Todo App (Problem 5)

Minimal Todo API implemented with TypeScript, Express and Mongoose.

Quick start:

```bash
cd src/problem5/todo_app
npm install
npm run dev
```

Run tests:

```bash
npm test
```

Run MongoDB with Docker:

```bash
docker compose up -d
```
